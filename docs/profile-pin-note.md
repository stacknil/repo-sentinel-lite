# Profile Pin Note

`repo-sentinel-lite` is worth pinning because it shows a compact, shippable
developer-security tool with real CI, release hygiene, and downstream
dogfooding.

Suggested ordering:
- Keep `telemetry-lab` ahead of it if you want your profile to lead with the
  broadest flagship project.
- Place `repo-sentinel-lite` ahead of `sec-writeups-public` when you want to
  emphasize practical tooling over published writeups.

Reusable profile blurb:

I build small, sharp developer-security tools and repository workflows.
`repo-sentinel-lite` is a deterministic Python CLI and pre-commit provider for
repository hygiene checks, lightweight secret scanning, and baseline-backed
validation.
