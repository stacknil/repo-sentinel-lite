# Dirty Example Repository

This intentionally small repository demonstrates findings that a reviewer may
choose to fix or baseline.

The `.env` value is a synthetic scanner fixture, not a real credential.
